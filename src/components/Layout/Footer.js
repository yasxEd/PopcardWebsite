import React from 'react';
import { Box, Typography } from '@mui/material';

const Footer = () => (
  <Box sx={{ width: '100%', textAlign: 'center', py: 2, mt: 4 }}>
    <Typography variant="caption" sx={{ color: '#9ca3af', fontSize: '0.85rem', letterSpacing: 1 }}>
      © yasxEd 2025
    </Typography>
  </Box>
);

export default Footer;
